# ==============================================================================
# 研报复现多智能体系统 - 核心依赖包 (支持 Python >=3.11, <3.13)
# ==============================================================================

# 1. CrewAI 核心框架及原生工具箱
crewai>=0.28.0
crewai-tools>=0.0.15

# 2. 核心大模型连接桥梁（你代码里显式 import 必须的包）
langchain-openai>=0.1.0
langchain-anthropic>=0.1.0

# 3. 基础生态支持
python-dotenv>=1.0.1
pyyaml>=6.0.1

# 4. 研报解析底层依赖（防止部分系统克隆后由于缺少解析器在运行时崩溃）
pypdf>=4.1.0
docx2txt>=0.8
